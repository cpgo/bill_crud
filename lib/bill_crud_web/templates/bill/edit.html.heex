<h1>Edit Bill</h1>

    <turbo-frame id={"bill-#{@bill.id}"}>
      <%= render "form.html", Map.put(assigns, :action, Routes.bill_path(@conn, :update, @bill)) %>
    </turbo-frame>


<span><%= link "Back", to: Routes.bill_path(@conn, :index) %></span>
